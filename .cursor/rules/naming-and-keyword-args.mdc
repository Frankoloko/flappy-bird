---
description: Prefer full variable names and keyword arguments at call sites
alwaysApply: true
---

# Naming and call style

- **Variable names:** Avoid abbreviated names (`g`, `ctx`, `tmp`) unless they are extremely common and universal (e.g. loop index `i`, coordinates `x`/`y`). Prefer descriptive names such as `game` for a `Game` instance.
- **Function calls:** When passing literals or when the parameter name adds clarity—especially boolean flags—use the **keyword** form (e.g. `take_action(flap=False)` not `take_action(False)`).

```python
# Prefer
game = Game()
game.take_action(flap=True)

# Avoid
g = Game()
g.take_action(True)
```
